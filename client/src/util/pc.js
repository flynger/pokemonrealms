var pokemonArr;

//recursion bad
// function filterPcAndGenerate(filteredArr, filters) {
//     if (filters.length == 0) {
//         return filteredArr;
//     } else {
//         let filteredPc = pokemonArr.filter(filters.length - 1);
//         filters.pop();
//         return filterPcAndGenerate(filteredPc, filters);
//     }
// }

function filterPcAndGenerate(filters) {
    // let filteredArr = pokemonArr;
    // for (let filter of filters) {
    //     filteredArr = filteredArr.filter(filter)
    // }
    // return filteredArr;
}
function getIconPath(pokemon) {

}

// function updatePc(pokemonUpdate) {
//     console.log("updating pc...");
//     pokemonArr = pokemonUpdate;
// }